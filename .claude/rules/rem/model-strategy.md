# Model & Effort Strategy

Default: `opusplan` model + `low` effort.

- `opusplan` auto-switches Opus during plan mode, Sonnet during execution
- For critical plans: manually `/effort high` before `/plan`
- Sharp review auto-analysis: delegated to `/sharp-review` skill
